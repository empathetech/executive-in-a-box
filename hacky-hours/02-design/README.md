# Design Documents

<!-- This folder contains the design documents that define how the product works.
     A new collaborator should be able to read these and understand how the product
     is meant to work. They don't need to be complete — they need to be honest.

     Not every project needs every document. Add what's useful, skip what isn't.

     Common documents:
       ARCHITECTURE.md      — system overview, components, external services
       DATA_MODEL.md        — data structures, relationships, storage
       USER_JOURNEYS.md     — how users move through the product
       STYLE_GUIDE.md       — visual language and UI conventions
       ACCESSIBILITY.md     — accessibility standards and commitments
       MARKET_FIT.md        — target users, differentiation, validation
       BUSINESS_LOGIC.md    — rules, calculations, domain-specific behavior
       SECURITY_PRIVACY.md  — auth, data handling, threat model
       LICENSING.md         — chosen license and dependency compatibility

     When a design decision changes, write an ADR in decisions/ rather than
     rewriting the original doc in place. -->

## decisions/

Architecture Decision Records (ADRs) — one file per significant decision,
named by date and topic: `YYYY-MM-DD-topic.md`
